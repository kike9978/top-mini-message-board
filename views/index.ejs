<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Título</title>
    <link rel="stylesheet" href="/style.css">
</head>

<body class="bg-yellow-400">
    <%- include("components/nav") %>

        <main>
            <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-2 mt-4 mx-4">
                <% messages.forEach(message=> { %>
                    <%- include("components/message", {message, formatDate}) %>
                        <% }) %>
            </div>
        </main>
</body>

</html>