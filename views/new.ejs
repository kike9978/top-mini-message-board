<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create new message</title>
    <link rel="stylesheet" href="/style.css">

</head>

<body>
    <%- include("components/nav") %>

        <h1>This is a new message</h1>
        <form action="/new" method="post" class="flex flex-col">

            <%- include("components/input", {label: "Username" , required: true, id:"username" }) %>
                <label class="flex flex-col">
                    <span class="font-bold">
                        Message
                    </span>
                    <textarea class="border border-solid border-slate-300 px-1" required id="messageText"
                        name="messageText" maxlength="200"></textarea>
                </label>
                max: 200 character
                <button>Add message</button>
        </form>
</body>

</html>