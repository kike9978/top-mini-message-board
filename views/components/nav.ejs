<nav>
    <ul class="flex flex-row gap-2 justify-end p-3 bg-slate-200">
        <% const links=[{href: "/" , text: "Home" } , {href: "/new" , text: "Post new message" }] %>
            <% links.forEach(link=> { %>
                <li>
                    <a href="<%= link.href %>">
                        <%= link.text %>
                    </a>
                </li>
                <% }) %>

    </ul>
</nav>